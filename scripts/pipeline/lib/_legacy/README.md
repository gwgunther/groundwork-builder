# Legacy pipeline modules (do not import)

Moved here in Phase 3 harden (2026-07). Live code must not import these.

| File | Replaced by |
|------|-------------|
| `ai-image-roles.js` | `assemble/bind-images.js` + `binding-to-image-roles.js` |
| `ai-silver.legacy.js` | `ai-silver.js` (parallel per-page) |

Kept for archaeology / one-off recovery only.
